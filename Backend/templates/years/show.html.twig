{% extends 'base.html.twig' %}

{% block title %}Years{% endblock %}

{% block body %}
    <h1>Years</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ year.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ year.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_years_index') }}">back to list</a>

    <a href="{{ path('app_years_edit', {'id': year.id}) }}">edit</a>

    {{ include('years/_delete_form.html.twig') }}
{% endblock %}
