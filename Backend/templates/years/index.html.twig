{% extends 'base.html.twig' %}

{% block title %}Years index{% endblock %}

{% block body %}
    <h1>Years index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Year</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for year in years %}
            <tr>
                <td>{{ year.id }}</td>
                <td>{{ year.year }}</td>
                <td>
                    <a href="{{ path('app_years_show', {'id': year.id}) }}">show</a>
                    <a href="{{ path('app_years_edit', {'id': year.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_years_new') }}">Create new</a>
{% endblock %}
