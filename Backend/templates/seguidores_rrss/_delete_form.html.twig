<form method="post" action="{{ path('app_seguidores_rrss_delete', {'id': seguidores_rrss.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seguidores_rrss.id) }}">
    <button class="btn">Delete</button>
</form>
