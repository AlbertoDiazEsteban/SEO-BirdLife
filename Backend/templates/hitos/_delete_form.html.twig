<form method="post" action="{{ path('app_hitos_delete', {'id': hito.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hito.id) }}">
    <button class="btn">Delete</button>
</form>
